    //Regular syntax    //Direct access from the Model Designermodel.Semaine.edgeDatesText = new Attribute("calculated", "string");model.Semaine.edgeDatesText.onGet = function() {     var result = '',    	currDate ='',    	currMonth = '',    	firstDateStr = '',    	lastDateStr = '',    	numWeek = this.SE_Num_Semaine,    	firstDate = this.SE_Date_Debut,    	lastDate = this.SE_Date_Fin;	//debugger;		currDate = '' + firstDate.getDate();	if (currDate.length < 2) {		currDate	= '0' + currDate;	}	currMonth = '' + (firstDate.getMonth() + 1);	if (currMonth.length < 2) {		currMonth	= '0' + currMonth;	}    firstDateStr = currDate + "/" + currMonth + "/" + firstDate.getFullYear();       	currDate = '' + lastDate.getDate();	if (currDate.length < 2) {		currDate	= '0' + currDate;	}	currMonth = '' + (lastDate.getMonth() + 1);	if (currMonth.length < 2) {		currMonth	= '0' + currMonth;	}	lastDateStr = currDate + "/" + currMonth + "/" + lastDate.getFullYear();            result = String(numWeek) + ' - du ' + firstDateStr + ' au ' + lastDateStr;        return result;}