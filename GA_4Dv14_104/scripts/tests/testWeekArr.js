	var numberOfWeeks = 21,		daysBefore = (Math.floor(numberOfWeeks / 2)) * 7,		daysAfter = (Math.ceil(numberOfWeeks / 2)) * 7,		d = new Date(2010, 9, 1),		firstWeekDate = new Date(d), 		lastWeekDate = new Date(d), //{},		weekArr = [];	firstWeekDate.setDate(d.getDate() - daysBefore);	lastWeekDate.setDate(d.getDate() + daysAfter);	weekArr = ds.Semaine.query("SE_Date_Fin >= :1 AND SE_Date_Debut <= :2", firstWeekDate, lastWeekDate).toArray("edgeDatesText, SE_ID, SE_Date_Debut, SE_Date_Fin");